.row
  .col-md-6
    = simple_form_for(project) do |f|
      = f.error_notification

      .form-inputs
        = f.input :name
        = f.input :email
        = f.input :channel_name
        = f.input :pm_slack_name, label: 'PM slack nickname'

      .form-actions
        = f.button :submit
